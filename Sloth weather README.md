# 懒懒天气 · 会看天的树懒（Lazy Weather Sloth）

把 FoloToy AI Passport 变成一只会看天的树懒。根据窗外实时天气，它会切换 8 种萌态形象（晴 / 多云 / 阴 / 雨 / 雪 / 雷阵雨 / 雾霾 / 大风），屏幕上同时显示城市、当前温度，并送上一句 8 字治愈语录。按 OK 还能手动预览每一只树懒；把手机里的照片传上来，它就秒变随身小相框，多图自动轮播。

> 本仓库为「固件发布仓库」，只提供烧录固件 `merge.bin`、一个开箱即用的 Web Bluetooth 配置小工具，以及使用方法，不含源代码。

---

## 📦 目录内容

| 文件 | 说明 |
|------|------|
| `merge.bin` | 完整固件（bootloader + 分区表 + 应用，从 0x0 一次性烧录） |
| `album_upload.html` | **单文件 Web Bluetooth 小工具**：改 WiFi、改城市、图片转格式 + 断点续传上传，全部搞定 |
| `README.md` | 本文件 |

---

## ⚙️ 烧录方法

`merge.bin` 是合并好的整片固件，**从地址 0x0 开始烧录**，一步到位，无需分步烧 bootloader / 分区 / 应用。

**工具推荐：ESP‑Flash（Flash Download Tool）**（Windows）

1. 打开工具，芯片选择 **ESP32‑C3**；
2. SPIDownload 模式，把 `merge.bin` 添加到地址 **0x0**；
3. 连接开发板，选择串口，波特率 115200；
4. 点击 START 烧录，完成后复位设备即运行。

**工具推荐：esptool**（命令行）

```bash
esptool.py --chip esp32c3 --port <串口> write_flash 0x0 merge.bin
```

> 首次刷机会覆盖原有固件。若之后想擦除所有数据（WiFi、城市、相册），可用 `esptool.py erase_flash`。

---

## 🔧 首次使用三步速通

1. **开机**：等待设备进入主界面；
2. **连 WiFi + 改城市 + 传照片**：全部用仓库自带的 `album_upload.html` 小工具，手机 Chrome / Edge 打开即可（需要 HTTPS 或 localhost 环境）；
3. **机身按键**：上 / 下切换城市；OK 进入应用 / 预览树懒；长按 OK 返回上级。

> 默认城市为合肥 / 上海 / 哈尔滨，WiFi 槽位 4 个，城市列表最多 12 个；所有配置**重启后自动保留**。

---

## 🧰 用小工具改 WiFi（最简方式）

仓库里的 `album_upload.html` 是一个 **纯前端 Web Bluetooth 工具**，手机 Chrome / Edge 直接打开就能用，不需要装任何 App。

### 操作步骤

1. 把 `album_upload.html` 下载到本地，**用手机浏览器打开**（必须是 Chrome / Edge，需要 HTTPS 或 localhost 安全环境；也可以在电脑上起一个本地 HTTP 服务，比如 `python3 -m http.server`，然后手机访问）；
2. 手机打开蓝牙，点页面顶部的 **「1) 连接设备 SLOTH‑WX」**，在弹出的列表里选 `SLOTH‑WX`；
3. 在 **「WiFi 配网」卡片**里填 SSID 和密码，点 **「写入并连接」**；
4. 设备会自动联网并存档，下次开机自动重连；底部日志区会显示连接状态。

> 固件内置 4 个 WiFi 槽位，可记忆多组，下次自动连第一个。想清空全部已存 WiFi，点卡片右下角的红色 **「清除已存 WiFi」**。

### 小工具实现原理（想了解细节可以看）

- WiFi 名称写入 BLE 特征 `a0e1f101`（SSID）
- 密码写入 BLE 特征 `a0e1f102`（PASS）
- 清空已存 WiFi 向命令特征 `a0e1f104` 写入 `0`
- 设备通过状态特征 `a0e1f105` 实时推送联网状态

### 备选方案：用第三方 BLE App（调试用）

如果不想开浏览器，也可以用 **nRF Connect**（iOS / Android）或 **LightBlue**：

1. 扫描并连接 `SLOTH‑WX`；
2. 找到 SSID 特征 → 写入 WiFi 名称；
3. 找到 PASS 特征 → 写入密码；
4. 设备自动联网，状态会通过 STATUS 特征通知。

---

## 🧰 用小工具改城市（最简方式）

还是同一个 `album_upload.html`，**内置了 50+ 城市的经纬度自动补全**（覆盖所有省会、直辖市、计划单列市），输城市名就行，不用查经纬度。

### 操作步骤

1. 按上一节的步骤连上设备；
2. 进入 **「天气城市」卡片**：
   - **看当前列表**：点 **「刷新列表」**，下拉框里会出现设备上已有的所有城市；
   - **切换城市**：在下拉框里选中一个 → 点 **「选择此城市」**，设备立即切换并拉取新天气；
   - **新增城市**：只填城市名（如 `广州`），经纬度**自动补全**；如果是国外城市或小城，手动填纬度 / 经度，点 **「添加城市到设备」**，自动刷新列表；
   - **删除城市**：在下拉框里选中要删的 → 点红色 **「删除所选城市」**。

### 城市写入协议（想了解细节可以看）

| 操作 | BLE 特征 | 写入内容 | 示例 |
|------|----------|----------|------|
| 新增 | `a0e1f108` (C_ADD) | `城市名,纬度,经度` | `南京,32.06,118.80` |
| 切换 | `a0e1f103` (C_CITY) | 城市下标 | `0` |
| 删除 | `a0e1f109` (C_DEL) | 城市下标 | `1` |
| 读列表 | `a0e1f10a` (C_LST) | — | 返回格式 `cnt\n0:名称@纬度@经度\n...` |

新增成功后自动切换；列表最多 12 个城市，**重启后保留**。

> 城市天气优先走国内源（中国天气网）；国外城市或精确到经纬度时，固件自动用 open‑meteo 拉取全球数据。

### 备选方案：第三方 BLE App

在 nRF Connect / LightBlue 里手动找上面的特征，按表格格式写入即可。

---

## 🧰 用小工具上传照片（自动转格式 + 断点续传）

这个是小工具最省事儿的地方——**所有格式转换、尺寸适配、断点续传全部自动搞定**，手机相册直接选图就能传。

### 操作步骤

1. 按前面的步骤连上设备；
2. 在 **「相册图片上传」卡片**里：
   - **目标槽位**：0~3，共 4 个槽位，分别对应设备相册里的 4 张图片；
   - **填充方式**：
     - `拉伸铺满`（stretch）——不管原图比例，强行铺满 240×320；
     - `等比留白`（contain）——保持原图比例，不够的地方补黑边；
   - 点 **「选择图片」**，从手机相册 / 文件里选一张（任意尺寸 / 格式，PNG / JPEG / WebP 都行）；
   - 手机会自动弹出预览 → 点 **「开始上传到设备」** → 进度条实时显示；
3. 看到绿色 `✔ 上传成功` 就完了，设备相册会自动刷新。

### 小工具帮你做了什么（背后的三步魔法）

**① 自动尺寸适配 + RGB565 格式转换**

设备屏幕是 240×320 的 RGB565（16 位色），但手机里的照片是 PNG / JPEG / WebP、各种尺寸、ARGB8888。小工具在浏览器里用 Canvas API 完成全部转换：

```
原图 → Canvas 缩放到 240×320 → 读取 RGBA → 压成 RGB565 (R5+G6+B5, 小端序) → Uint8Array (240×320×2 = 153600 字节)
```

填充方式由你在上一步选择；原图本身**不会被修改**，转换只发生在浏览器内存里。

**② 断点续传（BLE 最怕断连，这个是关键）**

BLE 传输容易中途断，小工具做了完整的续传机制：

1. **探测**：上传前先向设备发 `P:槽位号`，读取该槽位已落盘的字节数；
2. **判断**：如果已有完整的 153600 字节 → 直接跳过；如果只有一部分 → 发 `R:槽位号` 进入续传模式，从断点继续；否则发 `B:槽位号` 全新开始；
3. **分块写入**：每次 240 字节（BLE MTU 限制），分块直到写完；
4. **确认落盘**：写完后等设备回 `IMG_DONE` 才算成功，超时或收到 `IMG_ERR` 会提示重连重传。

中途断了也没关系——重连后再次点上传，小工具会自动探测断点，从上次断的地方继续。

**③ BLE 特征映射（想了解协议细节）**

| 步骤 | BLE 特征 | 方向 | 数据 |
|------|----------|------|------|
| 开始 / 续传命令 | `a0e1f106` (C_CTRL) | 写入 | `P:slot` 探测 / `B:slot` 全新 / `R:slot` 续传 |
| 图像数据 | `a0e1f107` (C_DATA) | 写入 | 240 字节 / 块，RGB565 小端序 |
| 状态通知 | `a0e1f105` (C_ST) | 通知 | `IMG_PROBE cnt offset` / `IMG_DONE` / `IMG_ERR ...` |

### 备选方案：不用小工具，手动转格式

如果你就是想手动折腾（或者在没有浏览器的环境），流程如下：

1. **把图片转成 240×320 的 RGB565 原始字节流**，每行小端序：
   - Linux/macOS 一行搞定：
     ```bash
     convert input.jpg -resize 240x320! -depth 5 rgb565:raw.bin
     ```
     （如果不想拉伸变形，把 `!` 去掉）
   - 或用 ImageMagick / GIMP 的 Raw RGB 导出，注意 **必须是小端序（little-endian）**，否则颜色会错；
2. 用 nRF Connect / LightBlue 连上设备；
3. 向 `a0e1f106` 写 `B:0`（0 换成你要的槽位号）开始；
4. 把 `raw.bin` 里的 153600 字节分块（每次 ≤ 240 字节）写入 `a0e1f107`；
5. 等设备回 `IMG_DONE` 确认。

> BLE 容易断，手动方式大概率会断。**强烈建议直接用小工具**，断点续传全自动。

---

## ❓ FAQ

**Q：看不到城市中文名？**
A：设备字库覆盖了省会、直辖市、计划单列市及港澳台等 41 个主要城市的汉字；其他城市会以拼音显示，属正常。

**Q：国外城市能显示吗？**
A：能。新增时填 `城市名,纬度,经度`，设备按经纬度经 open‑meteo 拉取全球天气；但国内城市天气源（中国天气网）不覆盖国外城市，仅依赖境外源。小工具的自动补全只覆盖国内主要城市，国外城市手动填经纬度即可。

**Q：浏览器小工具打不开 / 连不上？**
A：Web Bluetooth 只在 Chrome / Edge 桌面版和 Android 版可用，iOS Safari **不支持**。必须在 HTTPS 或 localhost 环境下打开 HTML 文件。

**Q：图片上传后设备没显示？**
A：检查：① 目标槽位是否选对（0~3）；② 填充方式是否合适（contain 会留白）；③ 日志区是否显示 `IMG_DONE`。如果中途断了，重连后小工具会自动从断点续传。

**Q：想恢复出厂？**
A：用 `esptool.py erase_flash` 擦除整片 flash，再重新烧 `merge.bin`。

---

## 🛡️ 固件校验

`merge.bin` 校验信息（用于核对下载是否完整）：

- 大小：3,085,920 bytes
- SHA‑256：`657934c84a87ec8ca1b176511c1b775129882bdf6e0d31eb0f5c34a98f22b8c8`

Windows 校验命令：`certutil -hashfile merge.bin SHA256`
